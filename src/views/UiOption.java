package views;

/**
 * Interface to represent the option to be selected by user at the start.
 */
public interface UiOption {
  /**
   * Method to get user's choice of UI at the start.
   *
   * @return char value representing user's choice.
   */
  char getUiOption();
}
